/**
 * App image assets
 *
 * Asset locations and expected dimensions:
 *   splash  → src/assets/images/splash.svg  (replace with splash.png,  605 × 944 px)
 *   welcome → src/assets/images/welcome.svg (replace with welcome.png, 990 × 1037 px)
 *   buddy   → src/assets/images/buddy.svg   (replace with buddy.png,   997 × 1036 px)
 *
 * To swap in real images:
 *   1. Drop the PNG next to the SVG placeholder (same folder, same base-name, .png extension)
 *   2. Update the import below to point to the .png file
 *   3. Delete the placeholder SVG
 */
export { default as splashImg } from './splash.svg';
export { default as welcomeImg } from './welcome.svg';
export { default as buddyImg } from './buddy.svg';
